{
  "scripts": {
    "build:css": "sass src/scss/custom.scss dist/assets/css/custom.css --style=compressed",
    "build:copy": "rimraf dist && mkdir -p dist && cpy static/ *.html *.xml .nojekyll robots.txt dist",
    "build": "npm run build:copy && npm run build:css"
  },
  "devDependencies": {
    "bootstrap": "^5.3.3",
    "cpy-cli": "^6.0.0",
    "rimraf": "^6.0.1",
    "sass": "^1.77.0"
  }
}
