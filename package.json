{
  "scripts": {
    "build:css": "sass src/scss/custom.scss dist/assets/css/custom.css --style=compressed",
    "build:copy": "mkdir -p dist && cp -r static/ *.html .nojekyll dist/ 2>/dev/null || true",
    "build": "npm run build:copy && npm run build:css"
  },
  "devDependencies": {
    "bootstrap": "^5.3.3",
    "sass": "^1.77.0"
  }
}

